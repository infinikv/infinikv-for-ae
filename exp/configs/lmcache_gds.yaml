# 256 Tokens per KV Chunk
chunk_size: 256
# Disable local CPU
local_cpu: false

max_local_cpu_size: 64
# Path to file system, local, remote or GDS-enabled mount
gds_path: "/mnt/lmcache_test/cache"
# CuFile Buffer Size in MiB
cufile_buffer_size: 16384
